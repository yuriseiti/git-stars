import React from 'react';
import { HeaderContainer } from './styles';

const Header: React.FC = () => {
    return (
        <HeaderContainer>
            234
        </HeaderContainer>
    );
};

export default Header;